<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para la cámara -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/> <!-- Opcional, solo hasta Android 9 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/> <!-- Opcional para leer fotos en galería -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Walkapaw"
        tools:targetApi="31">

        <activity
            android:name=".LogicaActivities.PaseoActivity"
            android:exported="false" />

        <activity
            android:name=".LogicaActivities.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".LogicaActivities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".LogicaActivities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".LogicaActivities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LogicaActivities.EditarPerfilActivity" />


    </application>

</manifest>
