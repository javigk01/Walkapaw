<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Walkapaw"
        tools:targetApi="31">

        <activity
            android:name=".logic.LoginAndRegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".logic.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".logic.PaseadorHomeActivity"
            android:exported="false" />

        <activity
            android:name=".logic.MenuActivity"
            android:exported="false"
            android:parentActivityName=".logic.HomeActivity" />
        <activity
            android:name=".logic.SolicitarActivity"
            android:exported="false"
            android:parentActivityName=".logic.HomeActivity" />
        <activity
            android:name=".logic.ActividadActivity"
            android:exported="false"
            android:parentActivityName=".logic.HomeActivity" />
        <activity
            android:name=".logic.CuentaActivity"
            android:exported="false"
            android:parentActivityName=".logic.HomeActivity" />

        <activity
            android:name=".logic.EditarPerfilActivity"
            android:exported="false"
            android:parentActivityName=".logic.CuentaActivity" />

        <activity
            android:name=".logic.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".logic.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".logic.PaseoActivity"
            android:exported="false" />

    </application>
</manifest>

